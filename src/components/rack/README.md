# components/rack

Displays the player's jamo pool as interactive tiles. Tap to rotate or decompose; drag to combine or place (drag added in UI-04).

## Exports

- `Rack` — renders all pool tiles from `useGame()` state; no props
- `Token` — single pool tile; handles tap interaction based on character type
