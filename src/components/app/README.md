# components/app

Root component — composes the full game UI and owns top-level layout, game initialization, viewport support messaging, and win state presentation.

## Exports

- `App` — root component. Renders a CSS-only unsupported viewport message below the documented minimum `375px × 667px` game viewport; otherwise renders the game shell.
