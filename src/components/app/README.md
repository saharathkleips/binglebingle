# components/app

Root component — composes the full game UI and owns top-level layout, game initialization, viewport support messaging, and win state presentation.

## Exports

- `App` — root component. Always renders the game shell with minimum tile hitboxes and lets the responsive flex layout use whatever viewport space is available.
