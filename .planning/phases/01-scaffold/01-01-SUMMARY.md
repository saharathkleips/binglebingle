---
phase: 01-scaffold
plan: 01
subsystem: infra
tags: [vite, react19, typescript, tailwindcss, vitest, playwright, oxlint, oxfmt, pnpm]

# Dependency graph
requires: []
provides:
  - Vite 8 + React 19 + TypeScript 6 project scaffold
  - Strict TypeScript config (noUncheckedIndexedAccess, exactOptionalPropertyTypes)
  - Tailwind CSS v4 wired via @tailwindcss/vite plugin
  - Vitest inline config in vite.config.ts (jsdom environment, globals)
  - Playwright config (chromium-only, /binglebingle/ base path)
  - pnpm scripts: dev, build, test, e2e, lint, fmt, fmt:check
affects: [02-jamo, 03-character, 04-word, 05-engine, 06-game, 07-state, 08-ui, 09-pwa]

# Tech tracking
tech-stack:
  added:
    - react@19.2.4
    - react-dom@19.2.4
    - vite@8.0.3
    - @vitejs/plugin-react@6.0.1
    - @tailwindcss/vite@4.2.2
    - tailwindcss@4.2.2
    - typescript@6.0.2
    - vitest@4.1.2
    - @vitest/coverage-v8@4.1.2
    - jsdom@29.0.1
    - "@playwright/test@1.58.2"
    - oxlint@1.57.0
    - oxfmt@0.42.0
    - "@types/node@25.5.0"
  patterns:
    - Named exports for React components (no default exports except main.tsx)
    - Colocated unit tests (src/**/*.test.ts alongside source files)
    - E2E specs in tests/ at repo root
    - Tailwind v4 CSS-first config (@import 'tailwindcss' in src/index.css, no tailwind.config.js)
    - Inline Vitest config in vite.config.ts (single config file)
    - noUncheckedIndexedAccess requires null-checks before DOM access

key-files:
  created:
    - package.json
    - pnpm-lock.yaml
    - vite.config.ts
    - tsconfig.json
    - tsconfig.app.json
    - tsconfig.node.json
    - index.html
    - src/main.tsx
    - src/App.tsx
    - src/index.css
    - src/vite-env.d.ts
    - src/App.test.tsx
    - playwright.config.ts
    - tests/smoke.spec.ts
    - .gitignore
  modified: []

key-decisions:
  - "Tailwind CSS v4 CSS-first: @import 'tailwindcss' in src/index.css, no tailwind.config.js (D-01)"
  - "TypeScript strict: noUncheckedIndexedAccess + exactOptionalPropertyTypes — all array/DOM access must be null-checked (D-02)"
  - "Vitest inline in vite.config.ts with /// <reference types='vitest/config' /> triple-slash (not separate vitest.config.ts)"
  - "Playwright workers: conditional spread pattern required for exactOptionalPropertyTypes compatibility"
  - "src/vite-env.d.ts with /// <reference types='vite/client' /> required for CSS side-effect imports under strict mode"
  - "@types/node added to tsconfig.node.json types for process.env in config files"

patterns-established:
  - "Named export pattern: export function App() — no default exports on components"
  - "Null-gate pattern: getElementById returns HTMLElement | null; always narrow before use"
  - "Config triple-slash: /// <reference types='vitest/config' /> in vite.config.ts for Vitest types"
  - "No path aliases: all imports use relative paths (./App, ../lib/...)"
  - "No barrel files: import directly from the file containing the export"

requirements-completed: [SCAF-01]

# Metrics
duration: 4min
completed: 2026-04-06
---

# Phase 01 Plan 01: Scaffold Summary

**Vite 8 + React 19 + TypeScript strict scaffold with Tailwind v4, Vitest, and Playwright wired — pnpm build and pnpm test both green**

## Performance

- **Duration:** ~4 min
- **Started:** 2026-04-06T03:51:58Z
- **Completed:** 2026-04-06T03:55:23Z
- **Tasks:** 5 (+ 1 checkpoint)
- **Files modified:** 15

## Accomplishments
- Complete project scaffold from zero: package.json, Vite, TypeScript, Tailwind v4, React 19
- Strict TypeScript config with noUncheckedIndexedAccess and exactOptionalPropertyTypes enforced
- Vitest inline config passing (1 sanity test green); Playwright config targeting /binglebingle/ base path
- `pnpm build` produces dist/ with no type errors; `pnpm exec tsc --noEmit` passes

## Task Commits

Each task was committed atomically:

1. **Task 1: package.json + Phase 1 deps** - `556b876` (chore)
2. **Task 2: vite.config.ts** - `ae352d3` (chore)
3. **Task 3: TypeScript configs** - `ce03e1c` (chore)
4. **Task 4: src/ entry points + index.html + .gitignore** - `b9d5b22` (feat)
5. **Task 5: Playwright config + smoke tests** - `fe2df9d` (chore)
6. **Fix: TypeScript strict mode build errors** - `0fc49d4` (fix)
7. **Fix: .tsbuildinfo in .gitignore** - `c2eb523` (fix)

## Files Created/Modified
- `package.json` - Project manifest with all Phase 1 deps and pnpm scripts
- `pnpm-lock.yaml` - Lockfile generated by pnpm
- `vite.config.ts` - Vite build config with Tailwind plugin, base path, and Vitest inline config
- `tsconfig.json` - Root TypeScript project references (app + node)
- `tsconfig.app.json` - Strict TypeScript config for src/ (noUncheckedIndexedAccess, exactOptionalPropertyTypes)
- `tsconfig.node.json` - TypeScript config for vite.config.ts and playwright.config.ts
- `index.html` - Entry HTML with lang=ko, div#root, module script
- `src/main.tsx` - React 19 entry with StrictMode, null-check guard before createRoot
- `src/App.tsx` - Minimal app component with Korean title (named export)
- `src/index.css` - Tailwind v4 CSS-first entry (@import 'tailwindcss')
- `src/vite-env.d.ts` - Vite client types for CSS side-effect imports
- `src/App.test.tsx` - Vitest sanity smoke test
- `playwright.config.ts` - Playwright config (chromium, /binglebingle/ base, webServer)
- `tests/smoke.spec.ts` - E2E smoke test checking page title and h1
- `.gitignore` - Standard ignores including *.tsbuildinfo

## Decisions Made
- Used `/// <reference types="vitest/config" />` triple-slash in vite.config.ts to get Vitest types without a separate vitest.config.ts
- Added `@types/node` to tsconfig.node.json `types` array for `process.env` access in config files
- Fixed `workers` typing with conditional spread (`...(CI ? { workers: 1 } : {})`) to satisfy `exactOptionalPropertyTypes`
- Added `src/vite-env.d.ts` to resolve CSS side-effect import TypeScript error (standard Vite pattern)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] TypeScript strict mode caused 4 build errors**
- **Found during:** Overall verification (pnpm build)
- **Issue:** (a) `test` property unknown on Vite defineConfig without Vitest types; (b) `process` undefined in tsconfig.node.json scope; (c) `workers: number | undefined` incompatible with `exactOptionalPropertyTypes`; (d) CSS side-effect import unresolved type
- **Fix:** Added `/// <reference types="vitest/config" />` to vite.config.ts; added `"types": ["node"]` to tsconfig.node.json; replaced `workers: undefined` with conditional spread; created `src/vite-env.d.ts`; installed `@types/node`
- **Files modified:** vite.config.ts, tsconfig.node.json, playwright.config.ts, src/vite-env.d.ts, package.json
- **Verification:** `pnpm build` exits 0, `pnpm exec tsc --noEmit` exits 0
- **Committed in:** 0fc49d4

**2. [Rule 2 - Missing Critical] *.tsbuildinfo files not gitignored**
- **Found during:** Post-build git status check
- **Issue:** `tsc -b` generates *.tsbuildinfo cache files which appeared as untracked — should never be committed
- **Fix:** Added `*.tsbuildinfo` to .gitignore
- **Files modified:** .gitignore
- **Verification:** `git status` shows no untracked tsbuildinfo files
- **Committed in:** c2eb523

**3. [Environment Limitation] Playwright browser binary not downloadable**
- **Found during:** Task 1 (playwright install step)
- **Issue:** Network is restricted in this container; playwright install chromium fails with EHOSTUNREACH
- **Fix:** Skipped browser download. `pnpm e2e` will require network access or pre-installed Chromium. The smoke test and configuration are complete; binary availability is an environment concern.
- **Impact:** E2E tests (`pnpm e2e`) cannot run in this container but will work in CI/CD environments with network access

---

**Total deviations:** 3 (2 auto-fixed bugs, 1 environment limitation documented)
**Impact on plan:** Auto-fixes corrected TypeScript strict mode compatibility issues. Playwright binary is an environment constraint that will resolve in network-accessible CI.

## Issues Encountered
- Playwright Chromium download blocked by container network restrictions (EHOSTUNREACH). Documented above.

## User Setup Required
None - no external service configuration required. Note: `pnpm e2e` requires Playwright Chromium to be installed (`pnpm exec playwright install chromium`) which requires network access.

## Self-Check: PASSED

All 9 created files verified on disk. All 7 task commits verified in git log.

## Known Stubs
None — this is an infrastructure-only phase. `src/App.tsx` contains the Korean title "빙글빙글" as intentional placeholder content that will be replaced by UI implementation phases.

## Next Phase Readiness
- Scaffold is complete and verified (build green, type-check green, unit tests green)
- All Phase 1 dependencies installed; subsequent phases can import React, use Tailwind utilities, and colocate tests immediately
- Phase 2 (jamo) can begin: create `src/lib/jamo/` with domain logic, add `.test.ts` files alongside

---
*Phase: 01-scaffold*
*Completed: 2026-04-06*
