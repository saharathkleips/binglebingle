---
phase: quick
plan: 260409-n3l
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/jamo/rotation.ts
  - src/lib/jamo/rotation.test.ts
  - src/lib/jamo/jamo.ts
  - src/lib/jamo/jamo.test.ts
  - src/lib/jamo/composition.ts
  - src/lib/jamo/composition.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "All six files compile with tsc --noEmit (strict mode)"
    - "pnpm test passes with no failures"
    - "ROTATION_MAP and COMBINATION_MAP are not exported — callers must use getNextRotation / composeJamo"
    - "Jamo-typed literals appear throughout — no raw string types on public API boundaries"
    - "reverse-map assertions (BY_INDEX) live inside their primary .each test, not in separate describe blocks"
    - "Rotation and combination rule tests live in their owning test files, not in jamo.test.ts"
  artifacts:
    - path: src/lib/jamo/rotation.ts
      provides: "getNextRotation with Jamo-typed params/return; ROTATION_SETS Jamo-typed; ROTATION_MAP unexported"
    - path: src/lib/jamo/jamo.ts
      provides: "BasicVowelJamo + ComplexVowelJamo types; BY_INDEX maps typed with specific Jamo subtypes"
    - path: src/lib/jamo/composition.ts
      provides: "CombinationRule with Jamo-typed inputs/output; COMBINATION_MAP unexported; composeSyllable/decomposeSyllable with Jamo subtypes"
  key_links:
    - from: src/lib/jamo/rotation.test.ts
      to: src/lib/jamo/rotation.ts
      via: "imports getNextRotation only (ROTATION_MAP import removed)"
      pattern: "import.*getNextRotation"
    - from: src/lib/jamo/jamo.test.ts
      to: src/lib/jamo/rotation.ts
      via: "ROTATION_SETS / ROTATION_MAP tests deleted from jamo.test.ts"
    - from: src/lib/jamo/composition.test.ts
      to: src/lib/jamo/composition.ts
      via: "COMBINATION_RULES / COMBINATION_MAP tests moved from jamo.test.ts"
---

<objective>
Apply the second batch of PR review suggestions across rotation.ts, rotation.test.ts, jamo.ts, jamo.test.ts, composition.ts, and composition.test.ts.

Purpose: Tighten the public API surface (unexport internal maps), propagate specific Jamo subtypes throughout, relocate misplaced tests to their owning modules, and consolidate redundant assertions.
Output: Six updated files that compile cleanly under strict TypeScript and pass all tests.
</objective>

<execution_context>
@/workspaces/binglebingle/.claude/get-shit-done/workflows/execute-plan.md
@/workspaces/binglebingle/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md

Relevant source (read before editing):
@src/lib/jamo/rotation.ts
@src/lib/jamo/rotation.test.ts
@src/lib/jamo/jamo.ts
@src/lib/jamo/jamo.test.ts
@src/lib/jamo/composition.ts
@src/lib/jamo/composition.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: rotation.ts + rotation.test.ts — tighten types, unexport map, complete test cases</name>
  <files>src/lib/jamo/rotation.ts, src/lib/jamo/rotation.test.ts</files>
  <behavior>
    - getNextRotation("ㅠ") returns "ㅕ"
    - getNextRotation("ㅕ") returns "ㅛ"
    - ROTATION_MAP is not exported (callers who try to import it get a TS error)
    - ROTATION_SETS type is readonly (readonly Jamo[])[]
    - getNextRotation return type is Jamo | null (not string | null)
    - test inputs are Jamo typed directly — no "as Jamo" casts needed
  </behavior>
  <action>
In rotation.ts:
1. Change ROTATION_SETS type annotation: `readonly (readonly Jamo[])[]`
2. Remove `export` keyword from ROTATION_MAP; change its type: `ReadonlyMap<Jamo, readonly Jamo[]>`
3. Change getNextRotation return type from `string | null` to `Jamo | null` — change the return expression's cast: `return set[(idx + 1) % set.length] as Jamo ?? null;` (or just rely on inference since ROTATION_SETS is now Jamo-typed)

In rotation.test.ts:
1. The import line currently imports `{ getNextRotation }` — remove the no-longer-exported `ROTATION_MAP` reference. Also update the import from `"./types"` to `"./jamo"` for the `Jamo` type if needed.
2. Change ROTATABLE_CASES type: `const ROTATABLE_CASES: [string, Jamo, Jamo][]` — with inputs typed as Jamo, the `as Jamo` casts on the test call can be dropped: `expect(getNextRotation(input)).toBe(expected)`
3. Add the 2 missing mid-set cases to ROTATABLE_CASES (the ㅑ→ㅠ→ㅕ→ㅛ set has 4 members but only ㅑ→ㅠ and ㅛ→ㅑ wrap are currently tested — add ㅠ→ㅕ and ㅕ→ㅛ):
   - `["ㅠ→ㅕ (index 1→2 in extended 4-set)", "ㅠ", "ㅕ"]`
   - `["ㅕ→ㅛ (index 2→3 in extended 4-set)", "ㅕ", "ㅛ"]`
4. NON_ROTATABLE type: `const NON_ROTATABLE: Jamo[]` — remove `as Jamo` cast on the test call.

Note: rotation.test.ts currently imports `ROTATION_SETS` and `ROTATION_MAP` from `./rotation` and `Jamo` from `./types`. Those rotation-structure tests (ROTATION_SETS/ROTATION_MAP describe blocks) live in jamo.test.ts right now. The review says to MOVE them out of jamo.test.ts into rotation.test.ts. After Task 2 removes them from jamo.test.ts, add them here. Do this in Task 1 so jamo.test.ts Task 2 can cleanly delete them. Move the full ROTATION_SETS describe block from jamo.test.ts into rotation.test.ts — it tests ROTATION_MAP internals (which is still accessible from within the module tests via an internal re-export or by keeping ROTATION_MAP exported at test-only scope).

IMPORTANT: ROTATION_MAP needs to be accessible in tests. The review says "don't export ROTATION_MAP" from the public API, but the existing tests in jamo.test.ts use it. Two options: (a) keep ROTATION_MAP exported but mark it `@internal` in JSDoc, or (b) move the ROTATION_MAP-using tests to use only the public `getNextRotation` API. Choose option (b): rewrite the ROTATION_MAP structural tests to use getNextRotation and ROTATION_SETS (which remains exported). The structural tests "ROTATION_MAP has entry for every jamo in every set" and "ROTATION_MAP entries do not include itself" can both be expressed via getNextRotation:
  - "every jamo in every set is rotatable" → `getNextRotation(jamo) !== null`
  - "result is never the input jamo" → `getNextRotation(jamo) !== jamo`

This means ROTATION_MAP truly stays unexported and tests are purely API-driven.
  </action>
  <verify>
    <automated>cd /workspaces/binglebingle && pnpm exec tsc --noEmit && pnpm exec vitest run src/lib/jamo/rotation.test.ts</automated>
  </verify>
  <done>rotation.ts compiles with ROTATION_MAP unexported and Jamo-typed; rotation.test.ts has 12 ROTATABLE_CASES (all 4-set members covered), no "as Jamo" casts, and structural ROTATION_SETS tests migrated from jamo.test.ts</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: jamo.ts + jamo.test.ts — add vowel subtypes, tighten BY_INDEX types, consolidate tests</name>
  <files>src/lib/jamo/jamo.ts, src/lib/jamo/jamo.test.ts</files>
  <behavior>
    - BasicVowelJamo type exists and is exported (the 10 basic vowels: ㅏㅑㅓㅕㅗㅛㅜㅠㅡㅣ)
    - ComplexVowelJamo type exists and is exported (the 11 complex vowels: ㅐㅒㅔㅖㅘㅙㅚㅝㅞㅟㅢ)
    - VowelJamo equals BasicVowelJamo | ComplexVowelJamo (refactored from inline union)
    - CHOSEONG_BY_INDEX has type Readonly<Record<number, ChoseongJamo>>
    - JUNGSEONG_BY_INDEX has type Readonly<Record<number, VowelJamo>>
    - JONGSEONG_BY_INDEX has type Readonly<Record<number, JongseongJamo | "">>
    - EXPECTED_CHOSEONG in tests uses type [ChoseongJamo, number][]
    - "CHOSEONG_BY_INDEX" describe block removed — reverse assertions merged into main .each test
    - ROTATION_SETS and COMBINATION_RULES describe blocks removed from jamo.test.ts (moved to owning files)
    - COMBINATION_MAP import removed from jamo.test.ts
  </behavior>
  <action>
In jamo.ts:

1. After the existing VowelJamo section (currently one union type), split into:
```typescript
export type BasicVowelJamo =
  | "ㅏ" | "ㅑ" | "ㅓ" | "ㅕ" | "ㅗ" | "ㅛ" | "ㅜ" | "ㅠ" | "ㅡ" | "ㅣ";

export type ComplexVowelJamo =
  | "ㅐ" | "ㅒ" | "ㅔ" | "ㅖ" | "ㅘ" | "ㅙ" | "ㅚ" | "ㅝ" | "ㅞ" | "ㅟ" | "ㅢ";

export type VowelJamo = BasicVowelJamo | ComplexVowelJamo;
```

2. Fix CHOSEONG_BY_INDEX type annotation:
`export const CHOSEONG_BY_INDEX: Readonly<Record<number, ChoseongJamo>> = Object.fromEntries(`

3. Fix JUNGSEONG_BY_INDEX type annotation:
`export const JUNGSEONG_BY_INDEX: Readonly<Record<number, VowelJamo>> = Object.fromEntries(`

4. Fix JONGSEONG_BY_INDEX type annotation:
`export const JONGSEONG_BY_INDEX: Readonly<Record<number, JongseongJamo | "">> = Object.fromEntries(`

In jamo.test.ts:

1. Update EXPECTED_CHOSEONG type: `const EXPECTED_CHOSEONG: [ChoseongJamo, number][] = [`
   Add the `ChoseongJamo` import from `"./jamo"`.

2. Merge the CHOSEONG_BY_INDEX describe block (lines 49-54) into the main EXPECTED_CHOSEONG .each test: add a third assertion in the existing `it.each` body:
   `expect(CHOSEONG_BY_INDEX[idx]).toBe(jamo);`
   Then delete the standalone CHOSEONG_BY_INDEX describe block.

3. Do the same for JUNGSEONG: add `expect(JUNGSEONG_BY_INDEX[idx]).toBe(jamo)` to the EXPECTED_JUNGSEONG .each test, delete the JUNGSEONG_BY_INDEX describe block.

4. Do the same for JONGSEONG: add `expect(JONGSEONG_BY_INDEX[idx]).toBe(jamo)` to the EXPECTED_JONGSEONG .each test, delete the JONGSEONG_BY_INDEX describe block. Also update EXPECTED_JONGSEONG type: `const EXPECTED_JONGSEONG: [JongseongJamo | "", number][]`. Add `JongseongJamo` to imports.

5. Update EXPECTED_JUNGSEONG type: `const EXPECTED_JUNGSEONG: [VowelJamo, number][]`. Add `VowelJamo` to imports.

6. Delete the entire `describe("ROTATION_SETS", ...)` block (lines 160-201) — those tests moved to rotation.test.ts in Task 1.

7. Delete the entire `describe("COMBINATION_RULES", ...)` block (lines 203-252) — those tests move to composition.test.ts in Task 3.

8. Remove the now-unused imports: `ROTATION_SETS, ROTATION_MAP` from `"./rotation"` and `COMBINATION_RULES, COMBINATION_MAP` from `"./composition"`.

Note on type assertion: `Object.fromEntries` returns `Record<string, V>` not `Record<number, V>`. TypeScript may complain about the cast. The current code uses explicit type annotation as an assertion — this is fine, keep the same pattern. If TS errors on the narrower types, cast explicitly: `as Readonly<Record<number, ChoseongJamo>>`.
  </action>
  <verify>
    <automated>cd /workspaces/binglebingle && pnpm exec tsc --noEmit && pnpm exec vitest run src/lib/jamo/jamo.test.ts</automated>
  </verify>
  <done>jamo.ts exports BasicVowelJamo and ComplexVowelJamo; all three BY_INDEX constants have specific Jamo subtype annotations; jamo.test.ts uses specific types in EXPECTED_* arrays, reverse-map assertions are inline, and all rotation/combination describe blocks are removed</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: composition.ts + composition.test.ts — Jamo types throughout, unexport map, table-driven tests</name>
  <files>src/lib/jamo/composition.ts, src/lib/jamo/composition.test.ts</files>
  <behavior>
    - CombinationRule.inputs type is readonly [Jamo, Jamo]
    - CombinationRule.output type is Jamo
    - COMBINATION_MAP is not exported
    - COMBINATION_MAP value type is Jamo (not string)
    - DECOMPOSE_MAP simplified with .map / flatMap
    - composeSyllable params: choseong: ChoseongJamo, jungseong: JungseongJamo, jongseong?: JongseongJamo (and return Jamo | null or string | null — keep string | null since it returns a composed syllable block, not a Jamo)
    - decomposeSyllable return: { choseong: ChoseongJamo; jungseong: JungseongJamo; jongseong: JongseongJamo | null } | null
    - composition.test.ts has table-driven tests covering all 27 COMBINATION_RULES for composeJamo
    - composition.test.ts has round-trip: composeJamo then decomposeJamo returns original pair
    - COMPOSE_CASES type: [Jamo, Jamo, Jamo | undefined, string][]
    - COMBINATION_RULES and COMBINATION_MAP describe blocks migrated from jamo.test.ts are now in composition.test.ts — update to not import COMBINATION_MAP (it's unexported); rewrite those assertions to use composeJamo instead
  </behavior>
  <action>
In composition.ts:

1. Update CombinationRule type:
```typescript
export type CombinationRule = {
  readonly inputs: readonly [Jamo, Jamo];
  readonly output: Jamo;
  readonly kind: "DOUBLE_CONSONANT" | "COMPLEX_VOWEL" | "COMPOUND_BATCHIM";
};
```

2. Update COMBINATION_RULES array to use Jamo literals (TypeScript will infer from the typed CombinationRule — the string literals should already satisfy Jamo, but add explicit `as const` if needed).

3. Remove `export` from COMBINATION_MAP. Change its type to `ReadonlyMap<string, Jamo>` (keys stay as `"a|b"` strings — a Map with tuple keys uses reference equality so string keys are correct; the review comment `ReadonlyMap<[Jamo, Jamo], Jamo>` is aspirational but not implementable with native Map). Simplify the IIFE builder using `.flatMap` per the review hint:
```typescript
const COMBINATION_MAP: ReadonlyMap<string, Jamo> = new Map(
  COMBINATION_RULES.flatMap((rule) => {
    const [a, b] = rule.inputs;
    const entries: [string, Jamo][] = [[`${a}|${b}`, rule.output]];
    if ((rule.kind === "DOUBLE_CONSONANT" || rule.kind === "COMPLEX_VOWEL") && a !== b) {
      entries.push([`${b}|${a}`, rule.output]);
    }
    return entries;
  })
);
```

4. Simplify DECOMPOSE_MAP builder using `.map`:
```typescript
const DECOMPOSE_MAP: ReadonlyMap<Jamo, readonly [Jamo, Jamo]> = new Map(
  COMBINATION_RULES.map((rule) => [rule.output, rule.inputs])
);
```

5. Update composeSyllable signature — add specific types. Import ChoseongJamo, JungseongJamo, JongseongJamo from "./jamo". Change params:
`choseong: ChoseongJamo, jungseong: JungseongJamo, jongseong?: JongseongJamo`
Remove the internal casts that used `as keyof typeof CHOSEONG_INDEX` since params are now typed. Keep return type as `string | null` (composed syllable is not Jamo).

6. Update decomposeSyllable return type to:
`): { choseong: ChoseongJamo; jungseong: JungseongJamo; jongseong: JongseongJamo | null } | null`
Update the return object to cast retrieved strings to their specific types: `choseong as ChoseongJamo`, `jungseong as JungseongJamo`, `jongseong as JongseongJamo`.

In composition.test.ts:

1. Update existing COMPOSE_CASES type:
`const COMPOSE_CASES: [Jamo, Jamo, Jamo | undefined, string][]`
Import `Jamo` (and `ChoseongJamo`, `JungseongJamo` if needed) from `"./jamo"`.

2. Add a full table-driven test for composeJamo covering all 27 combination rules. Import COMBINATION_RULES from `"./composition"`. Build the test data from COMBINATION_RULES directly:
```typescript
describe("composeJamo — all COMBINATION_RULES", () => {
  it.each(COMBINATION_RULES)(
    "$kind: $inputs → $output",
    ({ inputs, output }) => {
      expect(composeJamo(inputs[0], inputs[1])).toBe(output);
    },
  );
});
```

3. Add round-trip test for decomposeJamo:
```typescript
describe("decomposeJamo — round-trip", () => {
  it.each(COMBINATION_RULES)(
    "round-trip $kind: decomposeJamo(composeJamo($inputs)) returns inputs",
    ({ inputs, output }) => {
      expect(decomposeJamo(output)).toEqual([inputs[0], inputs[1]]);
    },
  );
});
```

4. Migrate the COMBINATION_RULES and COMBINATION_MAP describe blocks from jamo.test.ts. Since COMBINATION_MAP is now unexported, rewrite those assertions to use composeJamo:
   - "DOUBLE_CONSONANT: COMBINATION_MAP has ㄱ|ㄱ → ㄲ" → `expect(composeJamo("ㄱ", "ㄱ")).toBe("ㄲ")`
   - "COMPLEX_VOWEL: COMBINATION_MAP has ㅏ|ㅣ → ㅐ" → `expect(composeJamo("ㅏ", "ㅣ")).toBe("ㅐ")`
   - "COMPLEX_VOWEL: commutative for ㅗ+ㅏ" → `expect(composeJamo("ㅗ", "ㅏ")).toBe("ㅘ"); expect(composeJamo("ㅏ", "ㅗ")).toBe("ㅘ")`
   - "COMPOUND_BATCHIM: ㄱ|ㅅ → ㄳ" → `expect(composeJamo("ㄱ", "ㅅ")).toBe("ㄳ")`
   - "COMPOUND_BATCHIM: NOT reversed ㅅ|ㄱ" → `expect(composeJamo("ㅅ", "ㄱ")).toBeNull()`
   Keep count/structural assertions as-is (they only need COMBINATION_RULES, which remains exported).

5. After composeSyllable params become typed (ChoseongJamo etc.), the COMPOSE_CASES entries must also use valid Jamo subtypes. Verify "ㄸ" (DoubleConsonantJamo / ChoseongJamo) and "ㅃ" test cases still work — they should since DoubleConsonantJamo ⊂ ChoseongJamo.
  </action>
  <verify>
    <automated>cd /workspaces/binglebingle && pnpm exec tsc --noEmit && pnpm exec vitest run src/lib/jamo/composition.test.ts</automated>
  </verify>
  <done>composition.ts compiles with CombinationRule Jamo-typed, COMBINATION_MAP unexported, composeSyllable/decomposeSyllable using Jamo subtypes; composition.test.ts has full table-driven coverage of all 27 rules plus round-trip decompose test, and migrated structural tests from jamo.test.ts use composeJamo instead of COMBINATION_MAP direct access</done>
</task>

</tasks>

<verification>
Run the full test suite to confirm no regressions across all jamo modules:

```
cd /workspaces/binglebingle && pnpm exec tsc --noEmit && pnpm test
```

All tests should pass. Verify specifically:
- rotation.test.ts: 12 ROTATABLE_CASES (ㄱ↔ㄴ=2, ㅏ→ㅜ→ㅓ→ㅗ=4, ㅣ↔ㅡ=2, ㅑ→ㅠ→ㅕ→ㅛ=4)
- jamo.test.ts: imports only from "./jamo" (no rotation or composition imports)
- composition.test.ts: includes the full 27-rule table and the round-trip decomposeJamo suite
</verification>

<success_criteria>
- `pnpm exec tsc --noEmit` exits 0
- `pnpm test` exits 0 with all tests passing
- `grep -r "export.*ROTATION_MAP" src/lib/jamo/rotation.ts` returns empty
- `grep -r "export.*COMBINATION_MAP" src/lib/jamo/composition.ts` returns empty
- `grep -r "BasicVowelJamo\|ComplexVowelJamo" src/lib/jamo/jamo.ts` returns both types
- jamo.test.ts has no imports from ./rotation or ./composition
</success_criteria>

<output>
After completion, create `.planning/quick/260409-n3l-apply-pr-review-suggestions-rotation-jam/260409-n3l-SUMMARY.md`
</output>
