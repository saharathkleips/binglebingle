# Milestone: Screens and Shell

**Status:** Pending
**Affected Slices:** `src/components/shell/`, `src/components/how-to-play/`, `src/components/results/`

## Goal

The complete game shell assembled — NavBar, How to Play screen, Results modal, and dev settings panel.

## Execution Ledger

_(Agent: update these checkboxes as tests pass and local commits are made)_

- [ ] **UI-05** (`src/components/shell/`): Game shell renders NavBar and routes between game area and instructions screen
- [ ] **UI-06** (`src/components/how-to-play/`): How to Play screen explains the rotate → combine → compose mechanic to new players
- [ ] **UI-07** (`src/components/results/`): Results modal appears on win/loss; shows score and guess count summary
- [ ] **UI-08** (`src/components/shell/`): Dev settings panel (visible in dev mode only) allows overriding word selection strategy (daily / random / fixed word)

## Success Criteria

1. NavBar renders on every screen and routes between game area and How to Play
2. How to Play screen explains the mechanic clearly enough for a new player
3. Results modal appears on win, showing score and guess count
4. Dev settings panel visible in dev mode only; absent in production builds

## Agent Handoff

To execute this milestone:

1. Treat this file as your master state.
2. For each unchecked item in the Execution Ledger, load the `README.md` and `SPEC.md` for the specified slice.
3. Write the code, ensure tests pass, and create a local git commit.
4. Update the checkbox in this file to `[x]`. Do not push to remote.
