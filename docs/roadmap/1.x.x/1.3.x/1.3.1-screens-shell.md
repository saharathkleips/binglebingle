# Milestone: Screens and Shell

**Status:** Pending
**Affected Slices:** `src/components/NavBar.tsx`, `src/components/InstructionsScreen.tsx`, `src/components/Game.tsx`

## Goal

The complete game shell assembled — NavBar, InstructionsScreen, and dev settings panel. Win state is shown in-place (game area transforms on win; no separate results screen).

## Execution Ledger

_(Agent: update these checkboxes as tests pass and local commits are made)_

- [ ] **UI-05** (`src/components/NavBar.tsx`): NavBar renders on every screen with a button to toggle InstructionsScreen
- [ ] **UI-06** (`src/components/InstructionsScreen.tsx`): InstructionsScreen explains the rotate → combine → compose mechanic to new players; shown on first load and on demand
- [ ] **UI-07** (`src/components/Game.tsx`): On win, Pool area transforms in-place to show score (`calculateScore`) and target word; SubmitButton becomes an inert Share placeholder
- [ ] **UI-08** (`src/components/Game.tsx`, `src/App.tsx`): Dev settings panel visible only when `?dev=1` URL param is present; allows overriding word selection strategy (daily / random / fixed word)

## Success Criteria

1. NavBar renders on every screen and toggles InstructionsScreen
2. InstructionsScreen explains the mechanic clearly enough for a new player; shown on first load
3. On win, Pool area shows score and target word in-place (no modal/separate screen)
4. Dev settings panel visible only when `?dev=1` is in the URL; absent otherwise

## Agent Handoff

To execute this milestone:

1. Treat this file as your master state.
2. For each unchecked item in the Execution Ledger, load the `README.md` and `SPEC.md` for the specified slice.
3. Write the code, ensure tests pass, and create a local git commit.
4. Update the checkbox in this file to `[x]`. Do not push to remote.
