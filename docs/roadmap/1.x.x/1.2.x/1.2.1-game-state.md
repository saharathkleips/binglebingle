# Milestone: Game State

**Status:** Complete
**Affected Slices:** `src/context/game/`

## Goal

GameState and the full GameAction discriminated union typed; `gameReducer()` is a pure function that handles all actions correctly; GameContext and `useGame()` expose state and dispatch to React components.

## Execution Ledger

_(Agent: update these checkboxes as tests pass and local commits are made)_

- [x] **STAT-01** _(done in 1.1.3)_: `src/context/game/game.ts` already exists with `GameState`, `GameAction`, `PoolToken`, `SubmissionState`, etc. Verify it matches `src/context/game/SPEC.md` before proceeding.
- [x] **STAT-02** (`src/context/game/`): `gameReducer()` is pure (no async, no side effects); `createInitialGameState(word)` produces the initial state; correct submission slots stay filled after guess
- [x] **STAT-03** (`src/context/game/`): GameContext and `useGame()` hook expose game state and dispatch to the component tree

## Success Criteria

1. Every action type in the GameAction union is handled by the reducer without TypeScript errors
2. `gameReducer()` returns state unchanged for invalid actions (e.g. rotating a non-rotatable jamo)
3. After SUBMIT_GUESS with all-correct evaluation, correct slots remain filled and present/absent tokens return to pool
4. `useGame()` returns both state and dispatch; GameProvider gives components access to game state

## Post-Completion Refactor

After the milestone was marked complete, `game-reducer.ts` was split into focused handler modules to separate semantic concerns:

- `character-actions.ts` — ROTATE_TOKEN, COMBINE_TOKENS, SPLIT_TOKEN
- `submission-actions.ts` — PLACE_TOKEN, REMOVE_FROM_SLOT
- `round-actions.ts` — SUBMIT_GUESS, RESET_ROUND (also exports `buildInitialPool` / `buildEmptySubmission`)
- `game-reducer.ts` — shallow router delegating to the handlers above; still owns `createInitialGameState`

No type or export renames occurred — all public names remain correct, only file locations changed. All 444 tests continue to pass.

## Open Questions

- [ ] Should `fullDecompose` (or a new function in `src/lib/puzzle/`) be responsible for constructing the initial `PoolState` (characters + stable IDs), rather than returning a plain `Character[]` and leaving pool construction to the caller?

## Agent Handoff

To execute this milestone:

1. Treat this file as your master state.
2. For each unchecked item in the Execution Ledger, load the `README.md` and `SPEC.md` for the specified slice.
3. Write the code, ensure tests pass, and create a local git commit.
4. Update the checkbox in this file to `[x]`. Do not push to remote.
