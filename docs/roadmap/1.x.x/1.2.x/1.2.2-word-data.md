# Milestone: Word Data

**Status:** Pending
**Affected Slices:** `public/data/`

## Goal

words.json contains a working placeholder word list sufficient for end-to-end game play.

## Execution Ledger

_(Agent: update these checkboxes as tests pass and local commits are made)_

- [x] **DATA-01** (`public/data/`): words.json contains a small placeholder word list (at least 5 words across 3-, 4-, and 5-character lengths)

## Success Criteria

1. words.json contains at least 5 words
2. At least one word is 3 characters, one is 4 characters, and one is 5 characters
3. Every word passes `createWord()` validation (all Korean syllable blocks)
4. `loadWords()` can parse the file and `selectWord()` returns a valid Word from it

## Agent Handoff

To execute this milestone:

1. Treat this file as your master state.
2. For each unchecked item in the Execution Ledger, load the `README.md` and `SPEC.md` for the specified slice.
3. Write the code, ensure tests pass, and create a local git commit.
4. Update the checkbox in this file to `[x]`. Do not push to remote.
