# Milestone: Word and Pool

**Status:** Pending
**Affected Slices:** `src/lib/word/`

## Goal

The Word branded type, pool derivation pipeline, and word loading/selection implemented and tested.

## Execution Ledger

_(Agent: update these checkboxes as tests pass and local commits are made)_

- [ ] **WORD-01** (`src/lib/word/`): Word is a branded string type; `createWord()` validates the string is a non-empty sequence of Korean syllable blocks
- [ ] **WORD-02** (`src/lib/word/`): `derivePool()` decomposes a word into its constituent jamo; `normalizePool()` rotates each to the 0-index of its rotation set (obscuring the target word)
- [ ] **WORD-03** (`src/lib/word/`): `loadWords()` fetches `public/data/words.json`; `selectWord()` selects a word by strategy (daily date-seeded, random, or fixed)

## Success Criteria

1. `createWord()` rejects strings that are not Korean syllable blocks and accepts valid words like '한국어'
2. `derivePool('한국어')` returns the correct flat jamo array; `normalizePool()` rotates each jamo to the 0-index of its rotation set (e.g. ㄴ→ㄱ, ㅓ→ㅏ)
3. `loadWords()` fetches and parses words.json without error (tested with a fixture)
4. `selectWord()` with strategy 'daily' returns the same word for the same date; with 'random' it returns a valid Word
5. All exported functions have colocated Vitest tests that pass

## Agent Handoff

To execute this milestone:

1. Treat this file as your master state.
2. For each unchecked item in the Execution Ledger, load the `README.md` and `SPEC.md` for the specified slice.
3. Write the code, ensure tests pass, and create a local git commit.
4. Update the checkbox in this file to `[x]`. Do not push to remote.
