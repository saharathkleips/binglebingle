# Milestone: Character

**Status:** Complete (2026-04-11)
**Affected Slices:** `src/lib/character/`

## Goal

The Character type and its two core functions — `resolveCharacter()` and `isComplete()` — implemented and fully tested.

## Execution Ledger

- [x] **CHAR-01** (`src/lib/character/`): Character type `{ jamo: readonly string[] }` defined; `resolveCharacter()` reduces jamo list to combined jamo, syllable block, or null
- [x] **CHAR-02** (`src/lib/character/`): `isComplete()` returns true when `resolveCharacter` produces a syllable block in range U+AC00–U+D7A3

## Success Criteria

1. `resolveCharacter({jamo:['ㅏ','ㅣ']})` returns 'ㅐ'; `resolveCharacter({jamo:['ㅎ','ㅐ']})` returns '해'
2. `resolveCharacter({jamo:['ㄱ','ㅏ','ㄱ']})` returns '각'; `resolveCharacter({jamo:['ㄱ','ㅎ']})` returns null
3. `isComplete({jamo:['ㅎ','ㅏ','ㄴ']})` returns true; `isComplete({jamo:['ㄱ']})` returns false
4. All exported functions have colocated Vitest tests that pass

## Agent Handoff

To execute this milestone:

1. Treat this file as your master state.
2. For each unchecked item in the Execution Ledger, load the `README.md` for the specified slice.
3. Write the code, ensure tests pass, and create a local git commit.
4. Update the checkbox in this file to `[x]`. Do not push to remote.

